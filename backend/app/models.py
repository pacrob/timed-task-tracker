# Start minimal; add your models next.
from .db import Base
# e.g. from sqlalchemy.orm import Mapped, mapped_column
# class Example(Base):
#     __tablename__ = "examples"
#     id: Mapped[int] = mapped_column(primary_key=True)
